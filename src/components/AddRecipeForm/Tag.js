import styled from 'styled-components';

const Tag = styled.p`
  display: inline-block;
  margin: 0;
  padding: .7rem 0.8rem;
  color: #FFF;
  font-style: italic;
  letter-spacing: 0.1rem;
`;

export default Tag;
